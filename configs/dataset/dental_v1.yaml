name: dental
version: v1.3-dental-gcc
path: c:\Users\User\OneDrive\Desktop\VeloDoc\unified_dental_data_full.jsonl
eval_path: ""

validation:
  strategy: auto
  split_ratio: 0.1
  seed: ${run.seed}

format:
  type: jsonl
  fields: [id, source, prompt, completion]

processing:
  template_version: v1
  max_seq_len: 2048
  truncation: right
  packing: false
